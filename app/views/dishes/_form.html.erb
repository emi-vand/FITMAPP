<%= simple_form_for [@restaurant, @dish] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>

  <%= f.input :photo, as: :file %>
  <%= link_to "Back", ownerdashboard_path, class: "btn btn-secondary btn-sm" %>
  <%= f.submit class: "btn btn-primary btn-sm"%>
<% end %>


  <div class="list-group list-group-chatroom m-3">
      <% @chatrooms.each do |chatroom| %>
        <%= link_to "#{chatroom.name}", chatroom_path(chatroom), class: "card m-2 rounded list-group-item list-group-item-chatroom #{'active' if chatroom == @chatroom}" %>
      <% end %>
  </div>


